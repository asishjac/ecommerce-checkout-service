apiVersion: v1
kind: Service
metadata:
  name: checkout-service
spec:
  type: LoadBalancer # Triggers AWS ELB creation
  selector:
    app: checkout-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080